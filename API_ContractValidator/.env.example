# Authentication Configuration (disabled by default for local use)
MCP_AUTH_ENABLED=false
MCP_AUTH_TYPE=api_key
MCP_API_KEY=your-secret-api-key-here
MCP_JWT_SECRET=your-jwt-secret-here

# Rate Limiting Configuration (disabled by default for local use)
MCP_RATE_LIMIT_ENABLED=false
MCP_RPM=1000
MCP_BURST_LIMIT=100

# Server Configuration
DEMO_HTTP=false
